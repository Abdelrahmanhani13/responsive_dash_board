import 'package:flutter/material.dart';

class CusomDrawer extends StatelessWidget {
  const CusomDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Column(children: [

        ],
      ),
    );
  }
}
